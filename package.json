{
  "name": "retrieve-algo",
  "version": "1.0.0",
  "description": "Algorithm to retrieve books' files and details",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "yarn build && node build/index.js"
  },
  "author": "Ariel Rahmane",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.9.2",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
